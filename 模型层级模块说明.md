# MRAD 模型层级模块说明

## 一、项目概述

MRAD（Multi-Region Anomaly Detection）是一个基于 CLIP 模型的异常检测与分割框架。训练代码以 `train.py` 为入口，采用两阶段训练策略：第一阶段微调 patch 投影器和图像投影器（mrad-ft），第二阶段训练提示学习器和提示投影器（mrad-clip）。模型同时支持图像级异常分类和像素级异常分割。

---

## 二、模块依赖关系

```
train.py
  ├── AnomalyCLIP_lib          # 外部库：CLIP 模型加载与图像编码
  ├── mrad.py                  # 记忆库构建与评分计算（核心推理逻辑）
  │     └── models/mlp.py      # average_neighbor 邻域池化
  ├── models/
  │     ├── prompt_learner.py  # 可学习的提示学习器（文本端）
  │     └── mlp.py             # 投影器、MLP、邻域池化等组件
  └── utils/
        ├── loss.py            # 损失函数（FocalLoss, DiceLoss）
        ├── transforms.py      # 图像预处理变换
        ├── dataset.py         # 数据集加载类
        └── logger.py          # 日志记录器
```

---

## 三、模型层级架构（自上而下）

### 第一层：CLIP 视觉主干（ViT-L/14@336px）

- **来源**：AnomalyCLIP_lib 外部库
- **作用**：将输入图像（518×518）编码为多尺度特征
- **输出**：
  - `image_features`：图像级全局特征，维度 (B, 1024)
  - `patch_features`：多层 patch 特征列表，取索引 3（第 24 层的输出），维度 (B, 1369, 1024)，对应 37×37 个 patch
  - `patch_projections`：多层 patch 投影特征列表，取索引 3，维度 (B, 1369, 1024)
- **关键参数**：`features_list=[6,12,18,24]`，`DPAM_layer=24`
- **冻结状态**：整个训练过程中 CLIP 主干始终冻结（`model.eval()`, `torch.no_grad()`）

### 第二层：DAPM 模块（Dynamic Anomaly Prompt Module）

- **位置**：`model.visual.DAPM_replace(DPAM_layer=24)`
- **作用**：在 ViT 的第 24 层注入动态异常提示，增强异常区域的判别能力
- **输入**：ViT 中间层特征
- **输出**：增强后的特征，影响 `model.encode_image` 的所有输出

### 第三层：邻域特征平滑（average_neighbor）

- **位置**：`models/mlp.py` → `average_neighbor()`
- **作用**：对 patch 特征进行多尺度邻域平均池化（核大小 1×1, 3×3, 5×5 叠加取均值）
- **处理流程**：
  1. 将 token 序列 (B, 1369, C) 变形为 2D 特征图 (B, C, 37, 37)
  2. 分别用 1×1, 3×3, 5×5 核做平均池化
  3. 三个结果元素级求平均
  4. 还原为 token 序列 (B, 1369, C)
- **应用对象**：`patch_features[3]` 和 `patch_projections[3]`

### 第四层：记忆库模块（Memory Bank）

- **位置**：`mrad.py`
- **作用**：存储训练集中正常/异常样本的特征作为参考，用于推理时的相似度检索

#### 4.1 图像级记忆库（`build_cache_model`）

- **key**：所有训练样本的 CLIP 图像特征（L2 归一化后），维度 (N, 1024)
- **value**：对应标签的 one-hot 编码（正常=0, 异常=1），维度 (N, 2)
- **构建方式**：遍历训练数据，批量提取 CLIP 图像特征并拼接
- **缓存文件**：`cache_model_{dataset}.pt`

#### 4.2 Patch 级记忆库（`build_patch_cache_model`）

- **key**：逐样本按 ground truth mask 分离正常/异常 patch，各取均值并 L2 归一化，维度 (M, 768)
- **value**：对应标签 one-hot（正常=0, 异常=1），维度 (M, 2)
- **构建方式**：
  1. 提取第 24 层 patch 特征，经 average_neighbor 平滑
  2. GT mask 下采样到 37×37
  3. 对每个样本：pos_mask（异常区域）的 patch 取均值、neg_mask（正常区域）的 patch 取均值
  4. 收集所有样本的均值特征
- **缓存文件**：`cache_patch_model_{dataset}.pt`

### 第五层：投影器模块（Projector & MLP）

#### 5.1 Patch 投影器（`Projector`）

- **位置**：`models/mlp.py` → `Projector`
- **作用**：将 patch 特征从 1024 维投影到 768 维，分为两个投影（key 投影和 value 投影）
- **结构**：`nn.ModuleList([Linear(1024,768), Linear(1024,768)])`
- **forward 参数**：`proj(emb, idx)` → `self.fc[idx](emb)`
- **训练目标**：使 patch 特征与记忆库的相似度计算更准确

#### 5.2 图像投影器（`MLP`）

- **位置**：`models/mlp.py` → `MLP`
- **作用**：对图像级特征和记忆库 key 做对偶线性投影
- **结构**：两个独立的 Linear(768, 768)
- **forward**：`self.mlp1(emb1), self.mlp2(emb2)` — 分别投影查询特征和记忆库 key

#### 5.3 提示投影器（`AnomalyMLP`）

- **位置**：`models/mlp.py` → `AnomalyMLP`
- **作用**：将 patch 特征（正常/异常两端）映射为提示学习器的偏置
- **结构**：两个独立的 Linear(768, 768)
- **forward**：输入 `patch_emb1`（正常端）和 `patch_emb2`（异常端），分别投影后堆叠，输出维度 (B, 2, 768)

### 第六层：提示学习器（AnomalyCLIP_PromptLearner）

- **位置**：`models/prompt_learner.py` → `AnomalyCLIP_PromptLearner`
- **作用**：生成可学习的文本提示，使 CLIP 文本编码器产生面向异常检测的判别性文本特征
- **核心组件**：
  - `ctx_pos` / `ctx_neg`：可学习的正/负类上下文向量，维度 (1, 1, 12, 768)
  - `compound_prompts_text`：深层可学习提示嵌入，维度列表 [(4, 768), ...] × (depth-1)
  - `compound_prompt_projections`：将深层提示投影到 896 维
  - `mlp`：内置的 MLP（未在训练中使用）
  - `token_prefix/suffix`：前缀/后缀的 token embedding 缓冲区
- **forward 流程**：
  1. 接收来自 prompt_proj 的偏置 (B, 2, 768)
  2. 偏置加到 ctx_pos / ctx_neg 上（异常区域的上下文向量获得对应偏置）
  3. 拼接 prefix + ctx + suffix 形成完整 prompt embedding
  4. 返回 prompts（embedding 序列）、tokenized_prompts（token 序列）、compound_prompts_text（深层提示）

### 第七层：评分函数（Compute Score）

- **位置**：`mrad.py`

#### 7.1 图像级评分（`compute_socre`）

- **输入**：图像特征 (B, 1024)、记忆库 keys/values
- **流程**：
  1. 计算原始相似度矩阵（图像特征 × 记忆库 keys）
  2. 可选：通过 MLP 投影后重新计算相似度
  3. 基于 95% 分位数阈值屏蔽过高相似度（防止过拟合）
  4. Softmax 归一化
  5. 加权求和 → 输出分类 logits (B, 2)
- **温度系数**：logits / 0.07

#### 7.2 Patch 级评分（`compute_patch_socre`）

- **输入**：patch 特征 (B, 1369, 768)、patch 投影 (B, 1369, 768)、记忆库 keys/values
- **流程**：
  1. 计算 patch 与记忆库的原始相似度
  2. 通过 Projector 投影后重新计算相似度
  3. 基于 80% 分位数阈值屏蔽过高相似度
  4. Softmax 归一化
  5. 加权求和 → 输出分割 logits (B, 1369, 2)
  6. 非 mrad-ft 模式下额外计算异常/正常 token（用于后续提示学习器的偏置生成）

---

## 四、训练流水线

### 阶段划分

| 阶段 | 模型类型 | Epoch 范围 | 训练组件 | 损失函数 |
|------|----------|------------|----------|----------|
| Stage 1（FT） | mrad-ft / mrad-clip | epoch 0 ~ ft_epochs-1 | patch_proj, image_proj | seg_loss + image_loss |
| Stage 2（CLIP） | mrad-clip only | epoch ft_epochs ~ total-1 | prompt_learner, prompt_proj | clip_loss |

- `mrad-ft`：仅有 Stage 1，total_epochs = ft_epochs（默认 1）
- `mrad-clip`：Stage 1 + Stage 2，total_epochs = ft_epochs + clip_epochs（默认 1+5=6）

### Stage 1：微调阶段（FT）

```
训练组件: [patch_proj, image_proj]
冻结组件: [CLIP backbone, prompt_learner, prompt_proj]
优化器:   Adam, lr=0.0005, betas=(0.5,0.999)
调度器:   CosineAnnealingLR (仅 mrad-ft 使用)
```

**每一步的数据流**：

1. **特征提取**：`model.encode_image(image)` → image_features, patch_features, patch_projections
2. **邻域平滑**：对 patch_features[3] 和 patch_projections[3] 做 average_neighbor + L2 归一化
3. **分割评分**：`compute_patch_socre(patch_f_bia, cache_keys_patch, cache_values_patch, proj=patch_proj)` → seg_logit (B,1369,2)
4. **分割损失**：`seg_loss = FocalLoss(seg_map, gt) + DiceLoss(seg_map[:,1], gt)`
5. **分类评分**：`compute_socre(image_features, cache_keys, cache_values, proj=image_proj)` → logits (B,2)
6. **分类损失**：`image_loss = CrossEntropy(logits/0.07, label)`
7. **总损失**：`total_loss = seg_loss + image_loss`
8. **反向传播** → 更新 patch_proj 和 image_proj

### Stage 2：CLIP 对齐阶段

```
训练组件: [prompt_learner, prompt_proj]
冻结组件: [CLIP backbone, patch_proj, image_proj]
优化器:   Adam, lr=0.0005, betas=(0.5,0.999)
```

**每一步的数据流**：

1. **特征提取**：同上
2. **冻结 patch_proj**：`param.requires_grad = False`
3. **生成偏置**：`bias = prompt_proj(patch_f_bia[:,0,:], patch_f_bia[:,1,:])` → (B, 2, 768)
4. **生成提示**：`prompts, tokenized_prompts, compound_prompts_text = prompt_learner(bias=bias)`
5. **文本编码**：`text_features = model.encode_text_learn(prompts, ...)` → (B, 2, 768)
6. **多尺度 CLIP 对齐**：遍历 patch_projections 的指定层（从 feature_map_layer[3] 开始），计算 patch 特征与 text_features 的相似度图
7. **CLIP 损失**：`clip_loss = Σ FocalLoss(sim_map, gt) + DiceLoss(sim_map[:,1], gt) + DiceLoss(sim_map[:,0], 1-gt)`
8. **反向传播** → 更新 prompt_learner 和 prompt_proj

---

## 五、损失函数总览

| 损失函数 | 所在文件 | 用途 | 公式 |
|----------|----------|------|------|
| FocalLoss | utils/loss.py | 分割像素级损失（缓解正负样本不平衡） | -α(1-pt)^γ·log(pt)，γ=2 |
| BinaryDiceLoss | utils/loss.py | 分割区域级损失（关注重叠度） | 1 - 2·\|X∩Y\|/(\|X\|+\|Y\|) |
| CrossEntropy | torch 内置 | 图像级分类损失 | 标准交叉熵 |
| smooth | utils/loss.py | 平滑正则化（L2 邻域差） | λ₁·Σ(邻域差分)² |
| sparsity | utils/loss.py | 稀疏性正则化 | λ₂·‖arr‖（趋 0 或全 1） |

### Stage 1 损失组合

```
total_loss = FocalLoss(seg_map, gt) + DiceLoss(seg_map[:,1], gt)   ← seg_loss
           + CrossEntropy(logits/0.07, label)                      ← image_loss
```

### Stage 2 损失组合

```
clip_loss = Σ_layer [ FocalLoss(sim_map, gt)
                    + DiceLoss(sim_map[:,1], gt)
                    + DiceLoss(sim_map[:,0], 1-gt) ]
```

---

## 六、数据流完整示意图

```
                         输入图像 (B, 3, 518, 518)
                                   │
                    ┌──────────────┴──────────────┐
                    │   CLIP ViT-L/14@336px        │  ← 冻结
                    │   (含 DAPM 第 24 层注入)      │
                    └──────┬──────────┬───────────┘
                           │          │
              image_features    patch_features[3]   patch_projections[3]
              (B,1024)          (B,1369,1024)        (B,1369,1024)
                    │                │                      │
                    │        average_neighbor        average_neighbor
                    │          + L2 norm               + L2 norm
                    │                │                      │
                    ▼                ▼                      ▼
         ┌─────────────────┐  ┌──────────────┐    ┌──────────────┐
         │  compute_socre   │  │compute_patch  │    │  用于 Stage2  │
         │  (image_proj)    │  │_socre         │    │  CLIP 对齐    │
         └────────┬────────┘  │(patch_proj)   │    └──────┬───────┘
                  │           └───────┬───────┘           │
                  ▼                   ▼                   ▼
          图像级 logits         分割 logits         多尺度相似度图
          (B, 2)               (B, 1369, 2)        (B, 2, 37, 37)
                  │                   │                   │
                  ▼                   ▼                   ▼
          CrossEntropy       FocalLoss + DiceLoss   FocalLoss + DiceLoss
          (image_loss)          (seg_loss)            (clip_loss)
                  │                   │                   │
                  └─────────┬─────────┘                   │
                            │                             │
                     Stage 1 total_loss             Stage 2 clip_loss
```

---

## 七、关键超参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| model_type | mrad-clip | 训练模式：mrad-ft 仅 Stage 1，mrad-clip 两阶段 |
| ft_epochs | 1 | Stage 1 微调轮数 |
| clip_epochs | 5 | Stage 2 CLIP 对齐轮数 |
| learning_rate | 0.0005 | Adam 学习率 |
| batch_size | 8 | 批大小 |
| image_size | 518 | 输入图像尺寸 |
| n_ctx | 12 | 提示上下文长度 |
| t_n_ctx | 4 | 可学习文本嵌入长度 |
| depth | 9 | 可学习文本嵌入深度 |
| features_list | [6,12,18,24] | 使用的 ViT 特征层 |
| feature_map_layer | [0,1,2,3] | 特征图层索引 |
| seed | 111 | 随机种子 |

---

## 八、文件结构

```
MRAD/
  ├── train.py                    # 训练主入口（参数解析、两阶段训练循环）
  ├── mrad.py                     # 记忆库构建与评分计算
  ├── models/
  │     ├── prompt_learner.py     # 可学习提示学习器
  │     └── mlp.py                # 投影器、MLP、邻域池化
  ├── utils/
  │     ├── loss.py               # FocalLoss, DiceLoss, 正则化损失
  │     ├── transforms.py         # 图像预处理变换
  │     ├── dataset.py            # 数据集加载类
  │     └── logger.py             # 日志记录器
  ├── cache/                      # 记忆库缓存目录
  ├── checkpoints/                # 模型权重保存目录
  └── 模型层级模块说明.md          # 本文档
```
